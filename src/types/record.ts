export type RecordItem = {
  id: string;
  title: string;
  artist: string;
  year?: string;
  coverUrl?: string;
  genre?: string;
  style?: string;
  country?: string;
  labels?: string[];
  formats?: string[];
  notes?: string;
  description?: string;
  tracklist?: string[];
};

